# platform

**Stability:** experimental — provisional API, not recommended for production dependency.

Helper di piattaforma basato su `Platform.OS`.

## Import

```ts
import { isWeb, isIOS, isAndroid, isMobile } from "@personal-library/react-native-components";
```

## API

- `isWeb`, `isIOS`, `isAndroid`
- `isMobile()`
