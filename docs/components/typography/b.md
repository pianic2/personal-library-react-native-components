# B

**Stability:** beta — public consumer API, usable but contract may still change.

Wrapper di `Text` con `weight="bold"`.

## Import

```ts
import { B } from "@personal-library/react-native-components";
```

## Props

Uguali a `Text` (`ComponentProps<typeof Text>`).
