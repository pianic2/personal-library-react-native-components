# Small

Wrapper di `Text` con defaults: `size="sm"` e `variant="muted"`.

## Import

```ts
import { Small } from "@personal-library/react-native-components";
```

## Props

Uguali a `Text` (`ComponentProps<typeof Text>`).
