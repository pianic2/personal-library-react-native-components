# P

Wrapper di `Text` con defaults: `size="md"`, `weight="regular"`, `align="left"`.

## Import

```ts
import { P } from "@personal-library/react-native-components";
```

## Props

Uguali a `Text` (`ComponentProps<typeof Text>`).
