# shadows.base

Token shadow cross-platform.

## API

- `shadows` (`none`, `sm`, `md`, `lg`)
- `type Shadow = keyof typeof shadows`

## Note

- Contiene sia proprietà iOS/Web (`shadow*`) sia `elevation` (Android).
