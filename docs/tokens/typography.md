# typography.base

Token tipografici.

## API

- `fontFamily` (`regular`, `mono`)
- `fontSize` (`xs`..`xxxl`)
- `lineHeight` (`tight`, `normal`, `relaxed`)
- `fontWeight` (`regular`, `medium`, `semibold`, `bold`)
- `typography` (aggregato)
